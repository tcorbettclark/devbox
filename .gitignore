# Your baked-in SSH PUBLIC key (not secret, but identifies you).
# Drop it here with: cp ~/.ssh/id_ed25519.pub ./authorized_keys
authorized_keys

# Host mkcert root CA (incl. the private CA key) copied in by `devbox build`.
# Secret — never commit.
mkcert-ca/